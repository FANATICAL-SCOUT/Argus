# Core runtime
requests>=2.28

# Web dashboard (Phase 2+)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# Decoy scanning (optional — requires admin/root)
# scapy>=2.5

# Development / testing
pytest>=8.0
pytest-cov>=5.0
